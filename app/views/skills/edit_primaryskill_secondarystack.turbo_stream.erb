<%= turbo_stream.update @skill_target do %>
  <%= options_from_collection_for_select(@skill_set, :id, :skill, :none) %>  
<% end %>

<%= turbo_stream.update @stack_target do %>
  <%= options_from_collection_for_select(@stack_set, :id, :tech_stack, :none) %>  
<% end %>

<%= turbo_stream.update @ssc do %>
  <%= options_from_collection_for_select([],[],[]) %>  
<% end %>